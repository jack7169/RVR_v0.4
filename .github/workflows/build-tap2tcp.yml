name: Build tap2tcp

on:
  push:
    branches: [main]
    paths:
      - 'tap2tcp/**'
      - '.github/workflows/build-tap2tcp.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tap2tcp/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install aarch64 cross toolchain
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq gcc-aarch64-linux-gnu libc6-dev-arm64-cross

      - name: Build for aarch64
        working-directory: tap2tcp
        run: make cross CROSS_CC=aarch64-linux-gnu-gcc

      - name: Verify binary
        working-directory: tap2tcp
        run: |
          file tap2tcp-aarch64
          ls -la tap2tcp-aarch64

      - name: Copy to packages
        run: |
          mkdir -p packages/common
          cp tap2tcp/tap2tcp-aarch64 packages/common/tap2tcp-aarch64

      - name: Commit binary
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add packages/common/tap2tcp-aarch64 -f
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Build tap2tcp-aarch64 binary

          [skip ci]"
            git pull --rebase origin main
            git push
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tap2tcp-aarch64
          path: tap2tcp/tap2tcp-aarch64
